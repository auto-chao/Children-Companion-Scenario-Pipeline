# Contributing

- Do not commit secrets (`GEMINI_PROXY_API_KEY`, `HF_TOKEN`, etc.); use environment variables only.
- Main entrypoint is `bash main.sh`; keep any script/path updates consistent with it.
- Python style: `ruff` config is in `pyproject.toml`.
